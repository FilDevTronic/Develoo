<%= form_with(model: user_story, local: true) do |form| %>
  <% if user_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_story.errors.count, "error") %> prohibited this user_story from being saved:</h2>

      <ul>
      <% user_story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :as_a %>
    <%= form.text_field :as_a, id: :user_story_as_a %>
  </div>

  <div class="field">
    <%= form.label :isbat %>
    <%= form.text_field :isbat, id: :user_story_isbat %>
  </div>

  <div class="field">
    <%= form.label :so_that %>
    <%= form.text_field :so_that, id: :user_story_so_that %>
  </div>


    <%= hidden_field :commission, :commission_id, value: params[:id] %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
